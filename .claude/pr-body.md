<!-- ============================================================
PR 本文の型（配布物・全リポ共通。正本は loop-engineering の templates/pr-body.md）

このファイルを複製して埋め、--body-file で渡す:

    cp .claude/pr-body.md /tmp/pr-body.md
    # 中身を埋める（下の HTML コメントは消してよい）
    .claude/hooks/check-goal.sh > /tmp/goal.txt   # 出力を「ゴールの計測結果」に貼る
    gh pr create --title "..." --body-file /tmp/pr-body.md

PR ガード（.claude/hooks/guard-pr.sh）が機械チェックするのは次の 3 つ:
  1. --body-file であること（--body の直書きは拒否）
  2. 下の 6 節の見出しが揃っていること（該当が無い節は「なし」と書けばよい。消さない）
  3. check-goal.sh の出力がそのまま貼られていること（要約・加工をしない）

なぜこの型か: PR は人間がマージを判断する唯一の場所で、認知がボトルネックになる。
何をしたか一目で分かる形を固定しないと、マージ判断がただの承認作業になる。
「ゴールの計測結果」が自己申告ではなくスクリプトの出力であることが、この節の存在理由。

★拒否されたときは、stderr の指示どおり型を直して再実行すること。
  手動の PR 作成 URL に逃げない。PR ができていないのに TODO を完了にしない。
============================================================ -->

## 作ったもの

<!-- 何がどこまで到達したか。表が読みやすい: | 対象 | パス | 到達点 | -->

## ゴールの計測結果

<!-- check-goal.sh の出力をそのまま貼る。要約しない。加工しない -->

```
（ここに check-goal.sh の出力を貼る）
```

## 動作確認

<!-- 何をどう確かめたか。ブラウザ確認したならキャプチャを添付する
     （PR にキャプチャが貼られているから、人間は動いていることを確認できる）。
     未実施の確認は「未実施」と書く -->

## 情報源の食い違い

<!-- 情報源どうしで値が違ったもの。どちらが正しいかは勝手に決めず、両方の値と出所を書く。
     無ければ「なし」 | 項目 | A 側 | B 側 | どう扱ったか | -->

## 確認してほしい判断

<!-- マージ前に人間に見てほしい箇所。番号付きで。無ければ「なし」 -->

## 未解決の論点

<!-- 今回そのままにしたもの・次に回したもの。無ければ「なし」 -->

## レビューで却下した指摘

<!-- レビュー SA の指摘のうち採用しなかったものと、その理由。
     採用したものは差分に出るので書かなくてよい。無ければ「なし」 -->
